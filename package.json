{
  "name": "vellum-db",
  "version": "0.1.0",
  "description": "Structured storage for Vellum agents: tables via JSON Schema, query/aggregate via JSON, named saved queries.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "peerDependencies": {
    "@vellumai/plugin-api": "^0.8.0"
  },
  "vellum": {},
  "dependencies": {
    "@truto/sqlite-builder": "^2.0.1",
    "ajv": "^8.20.0",
    "nanoid": "^6.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/xlsx": "^0.0.36",
    "@vellumai/plugin-api": "^0.8.0",
    "typescript": "^5.9.3"
  }
}
